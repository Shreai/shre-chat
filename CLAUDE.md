# Claude Workspace Rules

Start with:

- `AGENTS.md`
- `../docs/shared-coding-rules.md`

Claude should use the shared contract as the default baseline for architecture review, flow reasoning, rollout safety, and trace-route review in `shre-chat`.
