# Gemini Workspace Rules

Start with:

- `AGENTS.md`
- `../docs/shared-coding-rules.md`

Gemini should use the shared contract as the default baseline for alternate review, risk finding, trace-packet review, and contract drift detection in `shre-chat`.
