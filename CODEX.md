# Codex Workspace Rules

Start with:

- `AGENTS.md`
- `../docs/shared-coding-rules.md`

Codex should use the shared contract as the default baseline for implementation, targeted verification, and route-to-UI contract alignment in `shre-chat`.
